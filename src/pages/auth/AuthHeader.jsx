import React from 'react'
import MainLogo from '../../images/artstore_light_logo.svg'

const AuthHeader = () => {
  return (
    <header id='auth-header' className='all-centered'>
        <img src={MainLogo} alt=".." />
    </header>
  )
}

export default AuthHeader